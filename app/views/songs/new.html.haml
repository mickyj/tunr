.row
  .small-5.columns
    = render(:partial => '/shares/errors')
    = form_for @song do |f|
      = f.label :name
      = f.text_field :name, :autofocus => true
      = f.label :image
      = f.text_field :image
      = f.submit nil, :class => 'button radius'
  .small-7.columns